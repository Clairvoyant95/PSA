import wave
import numpy
import scipy

my_file = wave.open("", rb) # otwieranie pliku

instrument_list = ['organ':range(), 13]

organ = (16 - 15804)
fortepian = range
#kontrbas . . .


fregiencies_in_my_signal = numpy.fft.freg...(my_file)

min_fre = fregiencies_in_my_signal.min
max_fre = max(fregiencies_in_my_signal)

my_signal_range = range(min_fre, max_freg)

if my_signal_range in organ:
    print "da sie zagrac na organie"
elif: my_signal_range


for instrument in instrument_list:
    if my_signal_range in instrument_list[:value]
        print "da sie zagrac na" + instrument[key:]
